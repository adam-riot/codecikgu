// Common types for CodeCikgu Playground

export interface Project {
    id: string
    title: string
    language: string
    code: string
    theme: string
    is_public: boolean
    created_at: string
    updated_at: string
    user_id?: string
  }
  
  